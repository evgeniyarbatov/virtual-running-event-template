[project]
name = "virtual-running-event-template"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gpxpy",
    "pandas",
    "requests",
    "polyline",
    "geopy",
]

[tool.uv]
package = false
