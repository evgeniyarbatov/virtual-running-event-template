# TODO

No CI — there's a real `tests/` suite (`make test`) but nothing runs it on push. Otherwise the Makefile/README/scripts are consistent and this template is in good shape (unlike some instances derived from this template, this repo does not commit Terraform state).
