# TODO

No CI — there's a real `tests/` suite (`make test`) but nothing runs it on push. Otherwise the Makefile/README/scripts are consistent and this template is in good shape (unlike `[private]`, an instance of this template, this repo does not commit Terraform state).
